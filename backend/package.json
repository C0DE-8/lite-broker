{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "db.js",
  "scripts": {
    "migrate": "node scripts/migrate.js",
    "test": "node scripts/test-local.js",
    "start": "node server.js",
    "dev": "nodemon --watch server.js --watch routes --watch middleware --watch utils --ext js,json server.js",
    "seed:local": "node scripts/seed-local.js",
    "db:create": "node scripts/create-database.js",
    "setup:local": "npm run db:create && npm run migrate && npm run seed:local",
    "test:workspace": "node scripts/test-workspace.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "moment": "^2.30.1",
    "multer": "^2.0.2",
    "mysql2": "^3.24.3",
    "nodemailer": "^7.0.12",
    "nodemon": "^3.1.11"
  },
  "engines": {
    "node": ">=20.17.0"
  }
}
